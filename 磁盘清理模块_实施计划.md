# C/D 盘磁盘清理模块 · 实施计划

> 目标：在现有「内存清理助手」中新增磁盘清理模块——显示 C/D 盘每块空间属于什么应用、有什么用，并安全清理垃圾文件。
> 本计划基于本机实测数据制定，不是通用模板。数据采集于 2026-09-18。

---

## 一、先厘清概念（避免做出来和预期不符）

「C/D 盘内存」准确说是**磁盘存储空间**（垃圾文件、缓存、大文件占的硬盘空间），跟此前做的 RAM 运行内存是两码事。本模块清理的是**硬盘**。

**本机实测现状**：

| 盘 | 卷标 | 总量 | 已用 | 剩余 | 文件系统 |
|---|---|---|---|---|---|
| C: | Windows-SSD | 400 GB | **318.4 GB** | 81.6 GB | NTFS |
| D: | Data | 551.6 GB | **417.9 GB** | 133.7 GB | NTFS |

**C 盘占用大头（实测）**：

| 目录 | 大小 |
|---|---|
| Users | **223.95 GB**（占总盘 56%） |
| Windows | 47.03 GB |
| Program Files | 42.04 GB |
| Program Files (x86) | 17.26 GB |
| ProgramData | 15.27 GB |

> 结论：C 盘满的根源在 **Users 目录**（用户数据、各应用缓存都在这），清理价值最高。

---

## 二、GitHub 调研结论

| 项目 | Stars | 语言 | 结论 |
|---|---|---|---|
| MangoDisk | 2845 | Rust/Tauri | ⚠️ 最接近（清理+分析+大文件+重复+卸载），但是完整独立应用、GPL 协议，不能直接嵌入 |
| SpaceSniffer | 2039 | — | ❌ 只读 Treemap 分析，无清理，已归档 |
| windows-cleaner-cli | 84 | Node | ⚠️ 只清缓存/临时文件，无「按应用显示空间」 |
| Jharu | 77 | Rust | ⚠️ 只清开发缓存（npm/pip/Docker），范围窄 |
| Temp-Cleaner | 55 | Batch | ❌ 纯批处理删临时文件 |

**结论：没有可直接拿来即用的项目。** 核心诉求「显示每块空间属于哪个应用、是什么用途」是现成工具都没做的。清理规则借鉴 MangoDisk 的分类思路，其余自研。

---

## 三、分步实施计划（共 5 步）

```
第1步 磁盘扫描引擎 ──┬──> 第2步 垃圾定位器（分类词典）──┐
                    │                                  ├──> 第4步 清理执行层 ──> 第5步 界面集成
                    └──> 第3步 空间占用分析（按应用归类）┘
```

### 第 1 步：磁盘扫描引擎（数据地基）

**产出文件**
- `server/collectors/diskScan.ps1` — PowerShell 扫描脚本
- `server/collectors/diskSpace.js` — Node 封装
- `server/services/diskService.js` — 汇总服务
- `server/services/__tests__/disk.test.js` — 单元测试

**要做的事**
1. 读 C/D 盘总量/已用/空闲（`Win32_LogicalDisk`）
2. 扫描一级目录大小（快速，不逐文件递归）
3. 扫描已知垃圾路径的大小（Temp、缓存、回收站等）
4. **性能硬指标**：单盘扫描 ≤ 5 秒（全盘逐文件递归实测会超时，必须用 robocopy `/L` 或 PowerShell 并行 job 加速）
5. 输出统一 JSON 结构，供后续步骤和界面使用

**验收标准**
- C/D 盘数据与「此电脑」一致
- 一级目录大小合计接近盘已用量（误差 < 15%）
- 扫描超时路径自动降级，不卡死

### 第 2 步：垃圾定位器（分类词典）

**产出文件**：`data/junkDict.zh.json`

把能安全清理的垃圾分类，每类给中文用途说明：

| 类别 | 典型路径 | 用途 |
|---|---|---|
| 系统临时文件 | `C:\Windows\Temp` | 系统运行残留 |
| 用户临时文件 | `%TEMP%` | 各应用运行残留 |
| 浏览器缓存 | Chrome/Edge 缓存目录 | 网页缓存 |
| 回收站 | `$Recycle.Bin` | 已删除文件 |
| Windows 更新缓存 | `SoftwareDistribution\Download` | 更新包残留 |
| 缩略图缓存 | `AppData\Local\...ThumbCache` | 图片预览缓存 |
| 应用缓存 | 微信/QQ/抖音/夸克等 | 聊天与媒体缓存 |

**验收标准**：本机 Top 垃圾类别覆盖率 ≥ 90%，每条经路径实测核实。

### 第 3 步：空间占用分析（回答"每块空间属于什么应用"）

扫描 C/D 盘大目录，按「应用」归类显示谁占了多少。借鉴 MangoDisk 的 Treemap 思路，用目录大小 + 应用词典映射。

**产出文件**：`server/services/diskAnalyzer.js`

**验收标准**：能回答「C 盘 224GB 的 Users 里，哪些应用各占多少」。

### 第 4 步：清理执行层（安全删除）

**产出文件**：`server/services/diskCleanupService.js`

沿用 RAM 清理的六道闸门：
1. 默认 dry-run（只出计划，不删）
2. 保护清单（系统目录、正在使用的文件绝不删）
3. 二次确认
4. 删除前后对比真实释放量
5. 审计日志 `logs/disk-cleanup-YYYYMMDD.log`
6. 失败原因如实归类

**验收标准**：删 1 个临时目录测试，真实释放量与磁盘剩余空间变化一致。

### 第 5 步：界面 + 集成

**产出文件**：`内存清理助手.html` 加「磁盘清理」标签页

复用现有 UI 风格：盘概览卡片 + 空间占用列表（按应用）+ 垃圾清理面板（勾选 → 计划 → 确认 → 前后对比）。

---

## 四、工作量与排期

| 步骤 | 预估 | 风险 |
|---|---|---|
| 1 扫描引擎 | 0.5～1 天 | 性能（全盘递归超时） |
| 2 垃圾词典 | 0.5 天 | 覆盖率 |
| 3 空间分析 | 1 天 | Users 目录深且大 |
| 4 清理执行 | 0.5～1 天 | 误删风险 |
| 5 界面 | 0.5～1 天 | — |
| **合计** | **约 3～4.5 天** | |

**分批交付**：
- 第 1 批（第 1 步）：磁盘扫描引擎 ✅
- 第 2 批（第 2+3 步）：垃圾定位 + 按应用归类 ✅
- 第 3 批（第 4+5 步）：安全清理 + 界面集成 ✅

本机实测可安全清理合计 **19.16 GB**（默认勾选，不含微信/文档/游戏）。

---

## 五、明确不做的事

- ❌ 不做「一键深度清理」直接删（必须先看清单 + 确认）
- ❌ 不删系统目录、用户文档、正在使用的文件
- ❌ 不编造「清理后可释放 XX GB」——所有数字来自真实扫描
- ❌ 不碰回收站里用户可能想恢复的文件（默认不勾选，可选）
- ❌ 不格式化、不删分区、不动系统还原点
