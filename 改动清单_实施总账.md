# 内存清理助手 · 改动清单实施总账

> **对象项目**：`D:\清理\任务_0916_我需要做一个能帮我清理电\`（GitHub `jokermikel/memory-cleaner`，实施基线 HEAD `3d5f2bb`）
> **依据文档**：`D:\清理\改动清单.md`（13 项代码改动 + 5 项测试验证类）
> **实施周期**：2026-09-22 ~ 2026-09-23，共 5 轮
> **本文性质**：三轮实施报告（改动 01 / 02 / 03 / 04-07 / 08-13）的合并总账

---

## 一、结果摘要

| 维度 | 结果 |
|---|---|
| **改动项完成度** | **13 / 13 全部完成** |
| **单元测试** | **96 / 96 通过**（14 套件） |
| **定向 HTTP 验证** | **48 / 48 通过** |
| **端到端测试** | **150 / 150 通过**（420.3s） |
| **改动规模** | 19 个文件变更（18 修改 + 1 删除），**+806 / −175 行** |
| **测试资产增长** | 单测 45 → 96 例；定向 18 → 48 项；e2e 108 → 150 项 |
| **清单建议被推翻** | **4 处**（均基于实测，见第四节） |
| **遗留清理** | 清单内全部自动化项已收口；仅剩真机手工项 |

---

## 二、13 项完成总表

| # | 优先级 | 标题 | 关键改动 | 涉及文件 |
|---|---|---|---|---|
| 1 | P0 | 前端恒传 `force:true`，批量上限被绕过 | `force` 语义拆分为「强制结束」与「放行批量」两个独立参数；超限需显式 `acknowledgeBatchLimit` | `_template.html`、`cleanupService.js`、`memory.js` |
| 2 | P0 | 磁盘禁止路径仅对 C/D 盘生效 | 静态 `FORBIDDEN_PREFIXES` → `forbiddenPaths()` 按本机盘符动态生成（每盘 × 盘根 + 6 个系统目录） | `diskCleanupService.js` |
| 3 | P0 | HTTP 服务无鉴权 | 四层防护：Host 环回 + Origin 同源 + 令牌（写操作及非豁免读接口）+ Content-Type 必须 JSON | `server.js`、`_template.html` |
| 4 | P1 | 垃圾扫描盘符写死 `C:` | 改用 `SystemDrive`；`topDirs` 传哨兵名避免全盘目录枚举 | `junkLocator.js` |
| 5 | P1 | 工作集修剪 PID 复用防护缺回退 | 照搬 `cleanup.ps1` 的 `startTime` 解析回退分支，两处逻辑逐行一致 | `trimWorkingSet.ps1` |
| 6 | P1 | 磁盘应用映射表含本机硬编码 | `_meta` 标注「示例字典」+ 匹配规则；保留真实目录名（去版本号会导致失配） | `diskAppMap.json` |
| 7 | P1 | 释放量统计含自然波动 | 口径改为进程级可归因（结束进程用 `sumFreedBytes`，修剪用 `wsDelta`）；整机差值降级为 `systemDeltaBytes` 对照 | `cleanupService.js`、`workingSetService.js` |
| 8 | P2 | 垃圾词典 `note` 写死本机实测值 | 18 条改为「示例量级（因机器而异）」，保留量级参考与操作提醒 | `junkDict.zh.json` |
| 9 | P2 | CLI 与 Web 的 C 盘垃圾清单不一致 | 删除 CLI 自带的 `C_JUNK`（缺回收站），复用 `buildJunkPaths()` + 相同盘符过滤 | `disk-cli.js` |
| 10 | P2 | 磁盘应用列表硬截断无「显示全部」 | 加 `diskAppShowAll` + 按钮，对齐 RAM 页做法；重扫时重置 | `_template.html` |
| 11 | P2 | 服务入口注释与事实不符 | 首行注释如实描述「挂载全部写接口」 | `server.js` |
| 12 | P2 | 遗留调试脚本（死代码） | 删除 `_probePath.ps1`（硬编码一批本机 PID、全项目零引用） | `_probePath.ps1` |
| 13 | P2 | 清理面板注释与代码不符 | 修正注释（说明「两种模式都显示、功能仅服务模式可用」） | `_template.html` |

### 附带完成的非清单项

| 项 | 内容 |
|---|---|
| 遗留 A | 昂贵 GET（全盘扫描类）可被 `<img>` 触发 → 改为「默认需令牌 + 廉价接口白名单豁免」 |
| 遗留 B | `cleanup.ps1` 含非 ASCII 字节（em-dash）→ 改回 `--`，回归文件自身的「纯 ASCII」约定 |
| 遗留 C | `T1` 套件清单硬编码 9 个 → 改为自动发现（14 个），关闭清单「新增 5 套件未接入 e2e」 |
| 遗留 D | `T0.4b` 断言边界缺陷 + `git quotepath` 中文路径漏检 → 均已修 |

---

## 三、执行轮次与测试演进

| 轮次 | 内容 | 单元测试 | 定向 HTTP | 端到端 | 备注 |
|---|---|---|---|---|---|
| 1 | 改动 01（批量上限） | 50/50（9 套件） | 18/18 | **107/108** | 唯一失败为 `T0.4` 过期基线（实施前就是红的，非本次引入） |
| 2 | 遗留清理 + 改动 02（禁止路径） | 80/80（14 套件） | 23/23 | 110/110 | 重建 `baseline.json`，`T0.4` 转绿 |
| 3 | 改动 04~07（P1 四项） | 92/92 | 29/29 | 118/118 | 发现基线清单漏 10 个源码文件（含本轮目标），修复并改为 `git ls-files` 动态生成 |
| 4 | 改动 03（HTTP 鉴权） | 92/92 | 40/40 | 136/136 | 新增 e2e `T11`（16 项）；新增 `T0.4b` 基线完整性断言 |
| 5 | 遗留 A + 改动 08~13（P2） | 96/96 | 48/48 | **150/150** | `T0.4b` 暴露自身边界缺陷并修复；`T1` 改为自动发现 |

**最终态：三档全绿，零失败。**

---

## 四、清单建议被推翻的 4 处（均基于实测）

清单是**评审材料**而非权威结论。实施中对 4 条建议做了实测复核并改判：

### ① 改动 4：「`topDirs` 改为空数组」→ 驳回

**清单原话**：「第二个参数 `['tmp']` 是占位用的 `topDirs`，改为空数组（`[]`）」

**实测**：`diskScan.ps1` 在 `-TopDirs` 为空时会走 `Get-ChildItem` **枚举该盘全部一级目录并逐个 robocopy 量大小**——
本机 D 盘 78 个目录耗时 **38.5 秒**，而这些数据 `locate()` 根本不消费。

**改判**：盘符改 `SystemDrive`，`topDirs` 传哨兵名 `__cc_no_top_dirs__`（只让脚本试量一个不存在的目录，成本可忽略）。
语义从「依赖 `C:\tmp` 碰巧不存在」升级为「显式声明不量目录」。

### ② 改动 6：「`D:\IntelliJ IDEA 2025.3.1.1` 改为稳定父目录 `D:\IntelliJ IDEA`」→ 驳回

**实测**：`diskAnalyzer.matchApp()` 要求**精确相等或以 `prefix + 路径分隔符` 开头**。
真实目录名含空格+版本号，`D:\IntelliJ IDEA` **不匹配** `D:\IntelliJ IDEA 2025.3.1.1`
（`matchApp('D:\IntelliJ IDEA', maps)` 实测返回 `null`）。

**改判**：保留实际目录名，另加 `_示例` 字段标注「换机器请按实际安装目录名调整」。

同一项里清单第 3 点的担心（`D:\steam` 与 `D:\Steam++` 大小写混淆）经复核**本来就不成立**——
`matchApp` 带了分隔符后缀，天然躲开前缀陷阱（实测 `steamlike`、`youxi2` 均不误匹配）。

### ③ 改动 3：「复用 `build.js` 现有的占位符注入机制」→ 驳回

**实测**：该机制**不存在**。`build.js` 只有一行 `tpl.replace('__SNAPSHOT_DATA__', ...)`，
只能注入快照数据。更关键的是——**首页是构建期预生成的静态文件**，
构建时还没有服务实例，而令牌每次启动都不同，**根本不可能在构建期注入**。

**改判**：在**服务端返回首页时**做替换。

### ④ 改动 13：「或按注释改行为（本地文件模式下隐藏面板）」→ 驳回

**实测**：面板在两种模式下**都显示**，本地文件模式下只是禁用按钮 + 渲染引导文案
（「请运行 启动.bat 再从 http://127.0.0.1:7788/ 打开」）。
**隐藏面板会让用户连这句引导都看不到**，体验反而更差。

**改判**：采纳清单的另一个选项「修正注释」，把设计意图写清楚，避免后来者按字面把入口藏掉。

### 规律

4 处被推翻的建议中，**3 处出自清单的「或 / 可选 / 更保守」类备选方案**。
这类建议往往是「看起来更简单」的直觉解法，未经测试验证。
**实施时的正确顺序是：先读既有测试确立语义 → 再实测验证备选方案 → 最后才动手。**

---

## 五、四类「声明与实现不一致」的根因

回看 13 项，P0 三项本质是同一类问题的不同表现——**文档承诺与代码事实脱节**：

| 类型 | 表现 | 修复本质 |
|---|---|---|
| 闸门宣称存在但被绕过 | README 说「六道闸门」，第 5 道因前端恒传 `force:true` 而形同虚设 | 让文档承诺变成代码事实 |
| 边界宣称完整但有盲区 | 禁止路径只覆盖 C/D 盘 | 同上 |
| 服务宣称本机专用但无门槛 | 只绑 `127.0.0.1`，未防跨站请求 | 同上 |
| 数值宣称真实但含噪声 | 释放量取整机差值，混入自然波动 | 改为可归因口径 |

配套动作：把 **「六道闸门」统一改为「九道」**（README 3 处 + DESCRIPTION.md 4 处含英文），
并补齐枚举——**口径一致本身就是这类问题的一部分**。

---

## 六、改动文件总表（19 个）

### 生产代码（13 个）

| 文件 | 增删 | 改动性质 |
|---|---|---|
| `server/server.js` | +148 | 四层访问控制 + 令牌注入；首行注释修正 |
| `_template.html` | +190− | `apiFetch` 令牌包装（19 处）、批量上限交互、错误文案集中、显示全部、注释修正 |
| `server/services/cleanupService.js` | +91− | `assertBatchAllowed` 独立闸门 + `sumFreedBytes` 进程级口径 |
| `server/services/diskCleanupService.js` | +71− | 禁止路径动态生成（`forbiddenPaths`） |
| `server/services/junkLocator.js` | +27− | 盘符动态化 + 哨兵 `topDirs` |
| `server/services/workingSetService.js` | +22− | 释放量口径改 `wsDelta` |
| `disk-cli.js` | +30− | 复用服务端垃圾清单 |
| `data/junkDict.zh.json` | +39− | 18 条 note 改写 + `_meta` 增补 |
| `data/diskAppMap.json` | +10− | `_meta` 示例字典标注 |
| `server/collectors/trimWorkingSet.ps1` | +11− | PID 复用 `startTime` 回退 |
| `server/collectors/cleanup.ps1` | +1−1 | em-dash → `--`（纯 ASCII） |
| `server/routes/memory.js` | +2 | 透传 `acknowledgeBatchLimit` |
| `server/collectors/_probePath.ps1` | **−33（删除）** | 死代码 |

### 文档（2 个）

| 文件 | 改动 |
|---|---|
| `README.md` | +35− | 九道闸门、接口鉴权说明、CLI 说明、测试计数、词典口径 |
| `DESCRIPTION.md` | +8− | 闸门口径中英文同步（`eight gates` → `nine gates`） |

### 测试（4 个）

| 文件 | 增删 | 新增用例 |
|---|---|---|
| `server/services/__tests__/junk.test.js` | +60− | 盘符动态化、哨兵、note 无本机值、CLI 复用清单、死代码已删 |
| `server/services/__tests__/cleanup.test.js` | +102− | 批量上限 5 例 + 释放量口径 6 例 |
| `server/services/__tests__/diskCleanup.test.js` | +53− | 非系统盘系统目录被拦、只拦目录不拦子树、盘符动态枚举 |
| `server/services/__tests__/workingSet.test.js` | +47− | 两处 PID 复用逻辑一致、ps1 纯 ASCII、释放量口径 |

---

## 七、留下来的回归资产（可长期看住这些改动）

### 单元测试（96 例 / 14 套件）

从 45 例增至 96 例，**累计 +51 例**。需要说明的是，其中一部分并非「新增用例」，
而是**原先未纳入统计的套件被计入**（第五轮 `T1` 自动发现后，这 5 个套件也已进入 e2e）：

| 归属 | 例数 | 说明 |
|---|---|---|
| 既有的 5 个套件 | 26 | `safety`/`workingSet`/`diskIoGuard`/`systemMemory`/`cacheMigrate`——首轮只统计前 9 个套件（45 例）时它们被漏在外 |
| 改动 1 | 5 | 批量上限独立闸门（含「只传 force 不能放行」的回归断言） |
| 改动 2 | 4 | 禁止路径全盘符、只拦目录不拦子树、盘符动态枚举 |
| 改动 4~7 | 12 | 盘符动态化、哨兵、PID 复用一致、ps1 纯 ASCII、释放量口径 |
| 改动 8/9/12 | 4 | note 无本机值、CLI 复用清单、死代码已删 |
| **合计** | **51** | |

### 定向 HTTP 验证（48 项）

`_verify_http_gate.js`，自建 7798 端口，全部使用不存在的 PID / dry-run：

| 分段 | 覆盖 |
|---|---|
| A1~A5 | 产物一致性（模板与构建产物同步、常量一致） |
| B1~B6 | 服务可用性、`plan` 的 warning 文案 |
| C1~C6 | 三道原有闸门回归 + force 语义保留 |
| E1~E5 | 改动 2：禁止路径全盘符覆盖、不误伤白名单 |
| F1~F6 | 改动 4~7 四项 |
| G1~G13 | 改动 3 / 遗留 A：四层访问控制 + 豁免清单 |
| H1~H6 | 改动 8/9/10/12 |

### 端到端（150 项 / T0~T12）

| 段 | 覆盖 |
|---|---|
| T0 | 服务生命周期、**T0.4 源码哈希基线**、**T0.4b 基线清单完整性**、生产路径未被破坏 |
| T1 | 单元测试套件**自动发现**（14 个）+ 逐个执行 |
| T2~T4 | 只读采集、内存清理闸门、沙箱真杀（含 PID 复用防护实测） |
| T5 | 磁盘只读 + 禁止路径（含改动 2/4/7 回归） |
| T6 | 沙箱真删 |
| T7~T9 | HTTP 接口、CLI、界面静态校验 |
| T10 | 历史坑回归（R1~R12） |
| T11 | 访问控制（A1~A17） |
| T12 | P2 项（P1~P6） |

### 基线完整性双保险

- `T0.4`：所有基线文件 SHA256 一致（检测「本轮是否改动生产源码」）
- `T0.4b`：基线键集 ⊇ 工作区中 git 追踪的源码集（防「新增文件忘了重建基线」）
- 两者共用 `_baseline-rules.js` 的同一份筛选规则，避免规则漂移导致自证

---

## 八、剩余未完成（均为真机手工项）

这些**无法通过自动化验证**，需要人工在真机执行：

| 项 | 说明 |
|---|---|
| 界面点一遍新按钮 | 「修剪工作集」「缓存搬走」预检/确认、磁盘应用「显示全部」未做 UI 点击测试 |
| 真实缓存目录搬走 | 未对浏览器/游戏等生产路径执行，只在 `%TEMP%` 沙箱验证 |
| 重启后 junction 仍有效 | 按 NTFS 语义应仍在，未实际重启验证 |
| 大文件下载期间反复清理 | 未做蓝屏压力；高 I/O 时代码会拒绝清理，不能代替实测 |
| ≥24 小时循环 | 未挂机（立项时最关注的红线：Mem Reduct 蓝屏经历） |

另外两项**已知残留**（低危，已如实记录在 README 或报告中）：

1. **令牌是「抬高门槛」不是绝对隔离** —— 本机程序仍可读首页/本地文件获取令牌；强隔离需改用命名管道。
2. **昂贵 GET 已被令牌保护，但若攻击者能拿到令牌仍可触发** —— 这一层只能靠令牌本身。

---

## 九、产出物清单

### 报告（5 份 + 本总账）

| 文件 | 内容 |
|---|---|
| `改动清单.md` | 原始评审材料（13 项 + 5 项测试类） |
| `改动01_批量上限闸门_实施与测试报告.md` | 改动 1 |
| `改动02_磁盘禁止路径盲区_实施与测试报告.md` | 遗留清理 + 改动 2 |
| `改动04-07_P1四项_实施与测试报告.md` | 改动 4~7 + 基线盲区发现 |
| `改动03_HTTP鉴权_实施与测试报告.md` | 基线完整性断言 + 改动 3 |
| `改动08-13_P2收尾_实施与测试报告.md` | 遗留 A + 改动 8~13 |
| **`改动清单_实施总账.md`**（本文） | 合并总账 |

### 脚手架（不属于项目仓库）

| 文件 | 用途 |
|---|---|
| `_shim_childio.js` | **IO 转接层**：绕过本机「同步子进程匿名管道 EBUSY」限制 |
| `_baseline-rules.js` | 基线文件筛选规则（生成与校验共用，单一来源） |
| `_verify_http_gate.js` | 定向 HTTP 验证（48 项） |
| `_verify_baseline_cov.js` | 基线完整性正/负向验证 |
| `_check_html_js.js` | HTML 内联 `<script>` 语法校验（占位符预处理 + 属性名排除） |
| `_verify_diskmap.js` | 映射表匹配语义校验 |
| `任务_0919_.../run-tests.js` | 端到端运行器（150 项，T0~T12） |
| `任务_0920_.../regenerate-baseline.js` | 基线重建（`git ls-files` 动态生成） |

### 备份

`.tmp/bak_20260922_gaidong1|gaidong2|gaidong4/`、`.tmp/bak_20260923_gaidong3|p2/` —— 各轮改动前的原始文件。

### 沉淀的 Skill

| Skill | 内容 |
|---|---|
| `agent-childproc-pipe-workaround` | 本机子进程管道封锁的诊断与绕过（含判定表、诊断脚本、转接层） |
| `localhost-access-control` | 本机 HTTP 服务访问控制配方（威胁模型、四层防护、16 条验收断言） |

---

## 十、可复用的经验

1. **清单是评审材料，不是圣旨。** 4 处建议经实测被推翻，其中 3 处出自「或 / 可选 / 更保守」类备选方案。
   **先读既有测试确立语义 → 再实测验证 → 最后动手。**

2. **「看起来更简单」的方案往往有隐藏成本。** 改动 4 的「空数组」看起来最省事，实际引入 38 秒无用扫描。

3. **测试断言必须证明「能被触发失败」。** `T0.4b` 加了正/负向双重验证——正向确认覆盖完整，
   负向（人为删一个键、模拟新增文件）确认能抓到遗漏。**一条永远通过的断言等于没有。**

4. **量了再改，不要估。** 改动 4 的 38 秒、改动 6 的 `null` 返回、`diskAppMap` 的大小写匹配，
   全部是写探针实测出来的，没有一条靠推断。

5. **环境限制要先建立「判定表」。** 本机「同步子进程 + 匿名管道」被封，
   通过逐项实测（`stdio: inherit/ignore/文件句柄/异步 spawn`）锁定唯一受限组合，
   才写出可用的转接层——而不是反复试错。

6. **改动要留「看住它」的断言。** 每项改动都配了源码核对或行为断言（如「不得再用 `force` 放行批量」），
   防止将来被改回去。这类断言在 e2e 里长期生效。

7. **文档口径一致性是改动的一部分。** 「六道闸门」分散在 README/DESCRIPTION/注释多处，
   改机制时若不同步口径，下次评审还会发现同类问题。

---

*本总账基于三轮实施报告与实跑的三档测试结果合成，所有数字均为实测值。*
