# 内存清理应用 · 分步实施计划

> 目标：做一个能读取本机全部内存、按「应用」归组展示内存占用与用途、界面清晰、并能安全释放内存的桌面工具。
> 本计划基于**在本机实测得到的数据**制定，不是通用模板。所有数字均为 2026 年 9 月 16 日在本机 PowerShell / Node 实测所得。

---

## 一、先说结论：这个应用要做成什么

**一句话定位**：一个「内存体检仪 + 安全清理助手」——把 384 个进程的内存占用，翻译成「哪 20 个应用在吃你的内存、它们分别是干什么的、关掉哪个安全」，并支持一键优雅关闭 + 释放待机内存。

**必须先讲清楚的一件事（避免做出来和用户预期不符）**：
Windows 上真正能释放物理内存的办法只有一条——**让占用的程序退出**。市面上「内存清理软件」宣传的一键释放，本质是清空系统 Standby List（待机缓存）或压缩各进程的 Working Set。副作用是：这些缓存被清掉后，程序下次访问要重新读磁盘，**系统可能反而变慢**。所以本应用的清理动作分三类，按风险从低到高：

| 清理类型 | 释放量 | 风险 | 是否实现 |
|---|---|---|---|
| ① 优雅关闭应用（等同手动点关闭） | 大（实测可释放 GB 级） | 低，会丢未保存内容 | ✅ 核心功能 |
| ② 结束已无窗口的孤儿进程 | 中 | 低 | ✅ |
| ③ 清空 Standby List / 压缩工作集 | 中（本机实测缓存类约 0.8 GB） | 中，可能拖慢系统 | ⚠️ 做成可选，默认关闭并写明副作用 |

---

## 二、本机实测基线（方案设计的事实依据）

### 2.1 硬件与系统

| 项目 | 实测值 |
|---|---|
| 机型 | LENOVO 83LV |
| CPU | AMD Ryzen 9 8945HX with Radeon Graphics（32 逻辑核） |
| 物理内存 | 34,142,187,520 字节 = **31.80 GiB**（可用可见 32,561 MB） |
| 内存条 | 2 条，P0 CHANNEL A / B，各 16 GiB，**DDR5-5600，Samsung** |
| 操作系统 | Windows 10.0.26200（Windows 11 内核） |
| 页面文件 | C:\pagefile.sys，分配 4608 MB，当前使用 122 MB |
| 提交上限 | 37,169 MB |

> **口径提示**：`TotalPhysicalMemory` = 34,142,187,520 字节（硬件装机量）；`TotalVisibleMemorySize` = 32,561 MB（系统可见量，差额被核显与固件占用）。界面上要同时显示这两个数，否则用户会拿任务管理器对不上数而怀疑工具不准。

### 2.2 当前内存占用快照

- 已用：**14,562 MB** / 32,561 MB（约 44.7%）
- 空闲：17,971 MB
- 进程总数：384 个
- 所有进程 Working Set 合计：14,486 MB

### 2.3 Top 内存消耗者（单进程，实测）

| 应用 | PID | Working Set | 私有内存 | 厂商 / 用途 |
|---|---|---|---|---|
| douyin（抖音） | 30644 | 1065.1 MB | 1195.7 MB | 北京微播视界 |
| QQLive（腾讯视频） | 35340 | 1028.2 MB | 897.9 MB | Tencent |
| Memory Compression | 4508 | 790.5 MB | 2.4 MB | 系统内存压缩（！） |
| Steam++ | 29944 | 493.5 MB | 643.7 MB | Steam 加速工具 |
| MsMpEng | 6672 | 463.2 MB | 433.4 MB | Windows Defender 杀毒 |
| gopeed | 38192 | 242.3 MB | 317.4 MB | 下载器 |
| explorer | 15776 | 237.3 MB | 252.6 MB | Windows 资源管理器 |
| steamwebhelper | 2872 | 208.9 MB | 452.5 MB | Valve Steam 客户端 |
| QQ | 648 | 204.9 MB | 200.4 MB | 腾讯 QQ |
| CloseCode | 25088 | 196.2 MB | 122.2 MB | 本智能体 |

### 2.4 按应用归组后（这才是用户真正想看的视图）

| 应用 | 进程数 | 合计 Working Set |
|---|---|---|
| svchost（系统服务宿主） | 99 | 1802.3 MB |
| douyin | 8 | 1568.2 MB |
| QQLive | 5 | 1234.4 MB |
| msedgewebview2 | 19 | 888.4 MB |
| Memory Compression | 1 | 790.4 MB |
| Steam++ | 2 | 601.9 MB |
| CloseCode | 4 | 560.3 MB |
| steamwebhelper | 7 | 470.0 MB |
| msedge | 11 | 455.5 MB |
| MsMpEng | 1 | 446.4 MB |

**这张表暴露了 3 个必须在设计里解决的真实问题：**
1. **抖音 8 个进程合计 1.57 GB**，如果按进程一行显示，用户看到的是 8 行各一两百 MB，根本判断不出"抖音在吃我 1.5 个 G"——**必须按应用归组**。
2. **svchost 有 99 个实例合计 1.8 GB**，既不能合并成一行（用户不知道是哪个服务）、也不能拆成 99 行（没法看）——需要**「可折叠的聚合行」**交互。
3. **Memory Compression 占 790 MB 却是系统核心组件**，用户看到"这么大，删了它"就出大事了——**必须有风险分级和禁止结束名单**。

### 2.5 实测踩到的两个权限坑（决定方案路线）

| 坑 | 实测结果 | 方案对策 |
|---|---|---|
| 读不到进程可执行文件路径 | 384 个进程中 **225 个**（58.6%）在普通权限下拿不到 `Path` | ① 提权提示：应用启动时检测管理员权限并引导提权；② 降级策略：拿不到路径就用进程名归组 |
| 读不到进程用途描述 | `FileVersionInfo.FileDescription` 大量为空（实测仅 douyin / QQLive / explorer / steamwebhelper 等少数能读到） | 不能只依赖系统元数据，**必须自建中文用途词典 + 多层兜底** |

---

## 三、分步实施计划（共 7 步）

依赖关系：第 1 步是所有后续步骤的地基；第 2、3 步可并行；第 5 步可先于第 4 步做（先只读不写，更快看到界面）。

```
第1步 采集引擎 ──┬──> 第2步 应用归组与用途词典 ──┐
                 │                                ├──> 第4步 清理执行层 ──> 第6步 打包交付
                 └──> 第3步 风险分级与保护名单 ───┘         ↑
                                                    第5步 界面层（可并行，先只读）
```

---

### 第 1 步：内存采集引擎（后端数据层）

**这一步要干的事**：把「整机内存全貌 + 每个进程的内存明细」稳定地采集成一份结构化 JSON，作为整个应用的唯一数据源。

**产出文件**
- `server/collectors/systemMemory.js` — 整机内存采集
- `server/collectors/processList.js` — 进程明细采集
- `server/services/memoryService.js` — 汇总成统一 JSON
- `server/routes/memory.js` — RESTful 接口
- `server/collectors/__tests__/*.test.js` — 单元测试

**具体任务**
1. **整机内存**：用 Node 内置 `os.totalmem() / os.freemem()`（实测 31.80 GB / 17.62 GB，与系统口径一致）拿基础值；再通过 PowerShell 调 `Win32_OperatingSystem` 拿 `TotalVisibleMemorySize / FreePhysicalMemory / TotalVirtualMemorySize / FreeVirtualMemory`，用于提交量与页面文件指标。
2. **每块内存条**：查 `Win32_PhysicalMemory`，返回 BankLabel / Capacity / Speed / Manufacturer / DeviceLocator。本机实测 2 条 × 16 GiB DDR5-5600 Samsung，界面按物理插槽位渲染成卡片。
3. **进程明细**：`Get-Process` 取 `WorkingSet64`（物理内存占用，对齐任务管理器）与 `PrivateMemorySize64`（私有提交量）。**两个指标都要采**——Working Set 体现"现在占了多少内存条"，Private 体现"这个程序向系统申请了多少"，回答"多大"时以 Working Set 为准并注明。
4. **性能计数器补充**：`Win32_PerfFormattedData_PerfProc_Process` 取 PageFaults / IO 读写字节，用于后续"是不是在狂读盘"的辅助判断。
5. **定义统一数据结构**（前后端契约，先定死再写代码）：
   ```json
   {
     "system": { "totalBytes": 0, "visibleBytes": 0, "freeBytes": 0, "usedBytes": 0,
                 "usedPercent": 0, "commitLimitBytes": 0, "commitUsedBytes": 0,
                 "pageFile": { "name": "", "allocatedMB": 0, "currentUsageMB": 0 },
                 "modules": [ { "bank": "", "capacityBytes": 0, "speedMhz": 0, "manufacturer": "" } ] },
     "apps":   [ { "key": "", "name": "", "processCount": 0, "workingSetBytes": 0,
                   "privateBytes": 0, "percentOfTotal": 0, "purpose": "", "vendor": "",
                   "risk": "safe|caution|protected", "processes": [ { "pid": 0, "workingSetBytes": 0, "path": null } ] } ],
     "collectedAt": ""
   }
   ```
6. **权限降级处理**：采 `Path` 时逐个 `try/catch`（实测 225/384 会失败），失败置 `null` 并打 `accessLimited: true` 标记，接口照常返回，不抛错、不中断整批采集。
7. **性能要求**：单次全量采集 **≤ 800 ms**。384 个进程逐条取 `MainModule` 实测很慢，改用「一次性 `Get-CimInstance` 批量取数 + 单次 PowerShell 调用返回 JSON」的方式，避免 384 次进程往返。
8. **单元测试**：验证 JSON 结构完整性、进程数 > 0、各字段单位换算正确、Permission denied 场景不崩。

**验收标准**
- 采集到的内存总量与任务管理器误差 < 1%
- 一次调用返回全部 384 个进程，无遗漏、无超时
- 接口在管理员与非管理员两种权限下都能返回数据（不报错）

**预估**：0.5～1 天

---

### 第 2 步：进程 → 应用归组 + 中文用途词典

**这一步要干的事**：把 384 条进程记录，压缩成「用户能看懂的应用列表」，并给每个应用配上一句人话说明「这是干什么的」。

**产出文件**
- `server/services/appGrouper.js` — 归组算法
- `data/appDict.zh.json` — 中文用途词典（手工维护）
- `data/protectedProcesses.json` — 见第 3 步
- `server/services/__tests__/appGrouper.test.js`

**具体任务**
1. **归组键设计（关键）**：不能只用进程名。规则按优先级：
   - ① 同 `ProcessName` 且 PE 文件签名/厂商一致的 → 合并为一个应用（如 8 个 douyin → 1 个应用）
   - ② 有名父子关系的（如 `steamwebhelper` 属于 `steam`）→ 通过 `Win32_Process.ParentProcessId` 向上找根进程，挂到根应用下
   - ③ `svchost.exe` 特殊处理：**99 个实例合并为 1 行「Windows 服务宿主 (99 个服务)」**，行可展开查看每个实例承载的具体服务名（从 `Win32_Service` 关联），并在展开项里标出各服务用途
   - ④ 拿不到路径的进程 → 退化为按 `ProcessName` 归组，并标记"信息受限"
2. **用途词典（三层兜底，按顺序命中）**：
   - 第 1 层：本地手工词典 `appDict.zh.json`，键为进程名/厂商组合，值为 `{ "purpose": "短视频客户端，用于刷抖音、直播", "category": "娱乐", "vendor": "北京微播视界" }`
   - 第 2 层：读 PE 元数据 `FileVersionInfo.FileDescription` + `CompanyName`（实测覆盖率低，仅作兜底）
   - 第 3 层：查不到就显示「未收录用途（进程名：xxx）」，**绝不允许编造用途**
3. **词典首批必须覆盖**（本机实测 Top 消耗者，逐个核实真实用途后录入）：
   douyin、QQLive、Memory Compression、Steam++、MsMpEng、gopeed、explorer、steamwebhelper、QQ、msedge、msedgewebview2、svchost、CloseCode、python、node 等，目标 **Top 50 应用覆盖率 ≥ 95%**。
4. **厂商与签名校验**：读数字签名的公司名，用于「同厂商多产品」聚合与风险判断（微软签名 vs 第三方 vs 无签名）。
5. **单元测试**：8 个 douyin 必须归成 1 条且合计 = 1568.2 MB；svchost 合并后仍能展开出 99 条子项；未知进程不抛错且标注"未收录"。

**验收标准**
- 所有 384 个进程 100% 被归入某个应用条目，无孤儿、无丢失
- 归组后合计内存 = 归组前合计内存（1568.2 MB 这类数字必须对得上，误差 0）
- Top 50 应用用途说明覆盖率 ≥ 95%，且每条都经人工核实

**预估**：1～1.5 天（词典是体力活，值钱的是这里）

---

### 第 3 步：风险分级与保护名单

**这一步要干的事**：回答「关掉哪个安全」。这是把工具从「看着酷」变成「敢用」的一步。

**产出文件**
- `data/protectedProcesses.json` — 禁止结束名单
- `server/services/riskClassifier.js` — 三色分级器
- `server/services/__tests__/riskClassifier.test.js`

**具体任务**
1. **三色分级规则**
   - 🔴 **protected 禁止结束**：结束会导致系统蓝屏/崩溃/失联。基于本机实测，至少包含：
     `System`、`System Idle Process`、`smss`、`csrss`、`wininit`、`winlogon`、`services`、`lsass`、`dwm`（结束即黑屏）、**`Memory Compression`（本机占 790 MB，绝对不能动）**、`MsMpEng`（Defender，关掉等于裸奔）、Windows 签名且承载核心服务的 `svchost` 实例、`Registry`、本应用自身。
   - 🟡 **caution 谨慎**：关掉不致命但会影响使用。如 `explorer`（关掉桌面和任务栏会消失）、输入法、云同步盘（未同步完会丢数据）、微信/QQ（可能丢未发出的消息）、开发工具（未保存代码）。
   - 🟢 **safe 可安全结束**：浏览器、视频客户端、下载器、游戏平台、已无窗口的后台残留。本机典型的可清理对象：`douyin`（1.5 GB）、`QQLive`（1.2 GB）、`steamwebhelper`、`gopeed` 等。
2. **名单数据来源**：以微软官方文档列明的系统关键进程为准，加上本机实测验证，**不允许凭印象拍脑袋写名单**。
3. **分级依据要可解释**：每条返回 `risk` 时附带 `reason` 字段（如"系统内存压缩组件，结束将导致系统异常"），界面上悬浮可见。用户不信任一个说不出理由的红绿灯。
4. **兜底规则**：未收录的进程默认给 🟡（不默认放行，也不默认禁止），并提示"未知应用，请自行确认"。
5. **单元测试**：`Memory Compression`、`csrss`、`lsass` 必须判为 protected；`douyin`、`QQLive` 必须判为 safe；未知进程必须判为 caution。

**验收标准**
- 本机 384 个进程全部分级完毕，protected 名单经逐条核实其官方来源
- 每个判定都能给出中文理由

**预估**：0.5～1 天

---

### 第 4 步：清理执行层（释放内存）

**这一步要干的事**：让「清理」按钮真的能释放内存，并且**每次都给用户看前后对比**。

**产出文件**
- `server/services/cleanupService.js`
- `server/routes/cleanup.js`
- `server/services/__tests__/cleanupService.test.js`

**具体任务**
1. **动作一：优雅关闭应用（默认主路径）**
   - 先 `CloseMainWindow()` 发关闭消息，等同用户点右上角 ×，让程序自己保存退出
   - 等待 3～5 秒，仍存活且是 🟢 应用才 `Stop-Process -Force`
   - 结束前**必须二次确认**，并列出「将关闭的清单 + 预计释放多少 MB + 可能丢失的未保存内容」
2. **动作二：清理孤儿进程**
   - 筛出 `MainWindowHandle == 0` 且无父进程且属于 🟢 的应用残留（本机实测有大量 msedgewebview2 / steamwebhelper 类残留），可批量清理
3. **动作三：清空 Standby List（可选，默认关闭）**
   - 需要 `SeProfileSingleProcessPrivilege` 权限，通过 PowerShell P/Invoke 调 `NtSetSystemInformation`（MemoryPurgeStandbyList）实现
   - 界面必须写明：「此项清理的是系统磁盘缓存，可能让程序变慢，不建议频繁使用」
4. **权限处理**：检测是否管理员，非管理员时禁用动作三并给出提权引导（第 1 步实测已证明普通权限拿不全信息）
5. **防护闸门（写进代码，不靠自觉）**
   - 拒绝操作 🔴 名单内的任何进程，返回明确错误而非静默跳过
   - 单次批量结束时如果目标 > 20 个进程，强制要求用户逐项勾选确认
   - 所有清理操作写审计日志 `logs/cleanup-YYYYMMDD.log`，记录时间/进程名/PID/释放量，可回溯
6. **效果量化**：每个动作执行前后各采一次内存快照，返回 `{ freedBytes, beforeUsed, afterUsed, actions: [] }`。**"释放了 1.2 GB" 这种话必须有前后实测数据支撑，不许估。**
7. **单元测试**：mock 进程接口后验证——保护名单内的进程调用结束会抛错；优雅关闭失败能降级到强制结束；审计日志正确落盘；未提权时动作三被正确拒绝。

**验收标准**
- 在真实进程上跑通一次完整清理，释放量与任务管理器观察一致
- 尝试结束 `lsass` 等保护进程时必须被拦截并给出中文原因
- 每次清理都有审计日志

**预估**：1～1.5 天

---

### 第 5 步：界面层（清晰、能用、好看）

**这一步要干的事**：把数据变成一眼能看懂的界面。用户三条要求里，「界面清晰」是明确写出来的，所以这步不是收尾，是正经一阶段。

**技术选型**：Vue 3 + TypeScript + Vite（符合项目规范：前端 Vue/React + TS，后端 Node RESTful）。同时额外产出一版**单文件 HTML 版本**（内嵌数据、双击即开、无需安装），方便直接发给别人看。

**产出文件**
- `web/src/views/DashboardView.vue` — 总览页
- `web/src/views/AppListView.vue` — 应用排行页
- `web/src/views/CleanupView.vue` — 清理页
- `web/src/components/MemoryRing.vue` — 环形内存仪表
- `web/src/components/MemoryModuleCard.vue` — 内存条卡片
- `web/src/components/AppTable.vue` — 应用表格（含展开聚合行）
- `dist/内存清理工具.html` — 单文件版本
- `README.md` — 使用说明

**具体任务**
1. **页面一 · 总览**
   - 大号环形图：`已用 14.5 GB / 共 31.8 GB（44.7%）`，中心显示可用值
   - 内存条卡片：2 张卡，各显示 16 GiB / DDR5-5600 / Samsung / 插槽位 P0 CHANNEL A、B
   - 指标条：已用、可用、提交量（37.2 GB 上限 / 当前已用）、页面文件
   - **同时标注两个口径**（32.56 GB 可见 vs 31.80 GB 硬件），避免和任务管理器对不上数
2. **页面二 · 应用排行（核心页面）**
   - 表格列：应用名 / 图标 / 内存占用 / 占比条形 / 进程数 / 用途说明 / 厂商 / 风险标签 / 操作
   - 默认按内存降序，支持搜索、按类别（娱乐/系统/开发/办公）筛选、按风险筛选
   - 聚合行可展开（解决 svchost 99 个实例的问题），展开后是子进程明细
   - 每行风险用色块 + 文字双编码（不只用颜色，色弱用户也能读）
   - 悬浮显示风险理由
3. **页面三 · 一键清理**
   - 勾选式批量选择，默认只勾 🟢 类
   - 清理前后对比图（横向条形对照：清理前 14.5 GB → 清理后 xx GB）
   - 结果清单：每个应用释放了多少，总计多少
4. **视觉规范**：卡片化布局、8px 圆角、主色 #3b82f6、背景 #f8fafc、间距统一 4 的倍数、跟随系统深色模式、窗口宽度自适应（1366px 笔记本到 4K 都不塌）
5. **定位检查**：Win11 实测为 10.0.26200，采用圆角无边框 + 自定义标题栏，风格与 Win11 一致
6. **不改字的信息设计原则**：任何"有多大"的数字，都要紧跟单位（GB/MB 自动换算）；任何"是什么"的说明，都必须来自第 2 步词典，不许界面上现编

**验收标准**
- 页面在任何窗口宽度下不出现横向滚动条、不遮挡、不重叠
- 深色模式下文字与背景对比度足够
- 关键数字与第 1 步采集结果一致
- 单文件版本用浏览器直接打开即可看到完整界面（含真实数据）

**预估**：1.5～2 天

---

### 第 6 步：打包交付

**这一步要干的事**：让用户能真的用上，而不是只看到一个源码目录。

三种交付形态，按优先级依次实现：

| 形态 | 优点 | 缺点 | 建议 |
|---|---|---|---|
| **A. 单文件 HTML + 本地 Node 服务** | 零安装、开发快、内置浏览器即可预览 | 需要 Node 环境 | ✅ 先做这个，把功能跑通 |
| **B. Electron 桌面应用** | 双击即用、能调系统 API、体验最像"应用" | 安装包 100 MB+、构建慢 | ✅ 功能稳定后做 |
| **C. PowerShell + 批处理脚本** | 无需任何运行时 | 无图形界面，不满足"界面清晰" | 作为应急备选 |

**具体任务**
1. 单文件版：把采集结果内嵌进 HTML，双击即可查看完整报告
2. Node 服务版：`npm run dev` 起服务，附 `启动.bat` 一键启动
3. Electron 版（可选）：打包成 exe，带管理员权限清单（解决第 1 步的权限坑）
4. 写 `README.md`：怎么启动、每个按钮干什么、为什么某些进程不能清、清理的副作用说明
5. 清理构建产物、确认没有把 node_modules 和临时文件一起交出去

**预估**：0.5～1 天

---

### 第 7 步：整体验收与上线前自检

| 验收项 | 标准 | 验证方式 |
|---|---|---|
| 内存总量准确性 | 与任务管理器误差 < 1% | 两边同时截图对比 |
| 进程覆盖完整性 | 384 个进程 100% 归组 | 程序内显示归组前后进程数 |
| 归组金额守恒 | 归组后合计 = 归组前合计 | 单元测试断言 |
| 用途说明覆盖率 | Top 50 应用 ≥ 95% | 人工过一遍列表 |
| 风险分级正确性 | protected 名单零误判 | 逐条核对官方文档 |
| 清理安全性 | 保护进程 100% 被拦截 | 手动尝试结束 lsass / Memory Compression |
| 释放量真实性 | 报告值与任务管理器实测一致 | 切换窗口前后对比 |
| 单元测试 | 关键函数覆盖率 ≥ 80% | 跑测试并出覆盖率报告 |
| 界面清晰度 | 无重叠/溢出，深色模式可读 | 多分辨率实测 |
| 管理员权限路径 | 提权与不提权都能正常启动 | 两种模式各跑一遍 |

---

## 四、工作量与排期

| 步骤 | 内容 | 预估 | 依赖 |
|---|---|---|---|
| 1 | 内存采集引擎 | 0.5～1 天 | — |
| 2 | 应用归组 + 用途词典 | 1～1.5 天 | 步骤 1 |
| 3 | 风险分级与保护名单 | 0.5～1 天 | 步骤 1 |
| 4 | 清理执行层 | 1～1.5 天 | 步骤 2、3 |
| 5 | 界面层 | 1.5～2 天 | 步骤 1（先只读）、步骤 2、3 |
| 6 | 打包交付 | 0.5～1 天 | 步骤 4、5 |
| 7 | 验收自检 | 0.5 天 | 全部 |
| | **合计** | **约 5.5～8.5 个工作日** | |

**建议的分批交付节奏**：
- **第 1 批（约 1 天）**：步骤 1 + 步骤 2，先出一个命令行版「内存占用排行」，让你马上能用上真实数据
- **第 2 批（约 1.5 天）**：步骤 3 + 步骤 5 的只读界面，能看不能清，先验证数据准确性和界面观感
- **第 3 批（约 2 天）**：步骤 4 + 步骤 6，加上清理能力和打包，交付完整应用

---

## 五、明确的风险与不做的事

**技术风险**
1. **权限**：实测 58.6% 的进程在普通权限下读不到路径。缓解：提权引导 + 降级策略，两条路都要通。
2. **采集性能**：384 个进程逐个取元数据很慢。缓解：批量采集，单次 PowerShell 调用返回 JSON，目标 ≤ 800 ms。
3. **"释放内存"存在反效果**：清 Standby List 会让系统变慢是已知副作用。缓解：做成可选、默认关闭、界面明说。
4. **进程随时变动**：采完到用户点清理之间进程可能已退出。缓解：执行前重新校验 PID + 启动时间，避免误杀复用 PID 的新进程。
5. **词典覆盖不全**：长尾应用查不到用途。缓解：如实显示"未收录"，绝不用 AI 现编用途糊过去。

**明确不做的事**
- ❌ 不做所谓"内存加速到 99%"这类无依据的宣传数字
- ❌ 不在未告知的情况下结束任何用户进程
- ❌ 不预置"一键清理"按钮直接开干——用户必须看到清单并确认
- ❌ 不编造进程用途，查不到就说查不到
- ❌ 不动虚拟内存/页面文件的系统配置

---

*本文档所有实测数据采集时间：2026-09-16，采集方式：本机 PowerShell (`Get-CimInstance` / `Get-Process`) 与 Node.js (`os` 模块)。*
