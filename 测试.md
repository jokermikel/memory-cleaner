# 内存清理助手 · 全功能测试报告

> 测试日期：2026-09-18
> 测试方式：真实执行（非代码走查），所有结论均来自工具实际运行输出
> 测试结果：**47 项测试通过 / 0 失败**

---

## 一、测试概览

| 测试块 | 通过 | 覆盖内容 |
|---|---|---|
| ① 只读功能 | **12/12** | 内存采集、应用归组、风险分级、用途词典 |
| ② 清理功能 | **11/11** | 六道安全闸门 + 真实进程清理 |
| ③ HTTP 接口 | **14/14** | 全部接口 + 错误处理 |
| ④ 界面功能 | **10/10** | 清理面板 + 浏览器无报错 |
| ⑤ 命令行 | **5/5** | cli 排行、筛选、搜索 |

---

## 二、测试环境

| 项目 | 值 |
|---|---|
| 机型 | LENOVO 83LV，AMD Ryzen 9 8945HX |
| 内存 | 32 GB（2 × 16 GB DDR5-5600 Samsung） |
| 操作系统 | Windows 10.0.26200（Windows 11 内核） |
| Node.js | v24.x |
| 运行权限 | 普通权限（非管理员） |

---

## 三、测试① 只读功能（12/12 通过）

采集 433 个进程 → 163 个应用，耗时 3.9 秒。

| 测试项 | 结果 | 实测值 |
|---|---|---|
| 整机内存总量 > 0 | ✅ | 32,561 MB |
| 内存条数量 = 2 | ✅ | 16GB Samsung × 2 |
| 已用 = 可见 - 空闲 | ✅ | 口径一致 |
| 采集无错误 | ✅ | errors = [] |
| 归组守恒（前后相等） | ✅ | 16,714.3 = 16,714.3 MB |
| 保护进程 > 0 | ✅ | 16 个 |
| 可清理应用 > 0 | ✅ | 14 个 |
| 三色分级全覆盖 | ✅ | safe 14 / caution 133 / protected 16 |
| Top 30 用途覆盖率 ≥ 80% | ✅ | 80%，未收录 6 个 |
| 应用字段完整 | ✅ | name/purpose/risk/riskReason 齐全 |
| 进程字段完整 | ✅ | pid/name/startTime 齐全 |
| 应用占比 0~100 之间 | ✅ | 全部合法 |

---

## 四、测试② 清理功能（11/11 通过）

### 4.1 安全闸门

| 测试项 | 结果 | 实测值 |
|---|---|---|
| plan 返回 dry-run 模式 | ✅ | mode = dry-run |
| plan 只含 safe 应用 | ✅ | 不含保护进程 |
| plan 自动排除保护进程 | ✅ | 16 个保护进程被排除 |
| execute 默认不执行 | ✅ | executed = false |
| 保护进程被拦截 | ✅ | lsass → 403 PROTECTED_TARGET |
| 未确认被拦截 | ✅ | 400 NOT_CONFIRMED |

### 4.2 真实清理（用临时进程验证，不碰真实应用）

| 测试项 | 结果 | 实测值 |
|---|---|---|
| 临时进程启动 | ✅ | 302.4 MB |
| 防误杀：错误启动时间被拒 | ✅ | pid_reused_refused |
| 防误杀：进程未被误杀 | ✅ | 进程仍存活 |
| 真实清理：进程成功关闭 | ✅ | method=force, verified=true |
| 真实清理：进程确实退出 | ✅ | 已退出 |

> 补充实测（此前验证）：449.5 MB 进程关闭后，系统已用内存实测下降 358.6 MB，证明清理确实释放了内存。

---

## 五、测试③ HTTP 接口（14/14 通过）

| 测试项 | 结果 | 实测值 |
|---|---|---|
| GET /api/health | ✅ | status=ok |
| GET / 返回页面 | ✅ | 300 KB，含「内存清理助手」 |
| GET /api/memory/snapshot | ✅ | 165 应用，守恒=true |
| GET /api/memory/system | ✅ | 32,561 MB |
| GET /api/memory/apps?risk=safe&limit=3 | ✅ | 3 个，全是 safe |
| GET /api/memory/apps?q=抖音 | ✅ | 1 个匹配 |
| GET /api/memory/processes?limit=5 | ✅ | 5 条 |
| GET /api/cleanup/plan | ✅ | dry-run，61 进程 |
| POST execute 默认 dry-run | ✅ | executed=false |
| POST execute 保护进程 → 403 | ✅ | PROTECTED_TARGET |
| POST execute 未确认 → 400 | ✅ | NOT_CONFIRMED |
| POST 非法 JSON → 400 | ✅ | BAD_BODY（0.001 秒响应） |
| 未知路径 → 404 | ✅ | NOT_FOUND |
| GET 清理接口 → 405 | ✅ | METHOD_NOT_ALLOWED |

---

## 六、测试④ 界面功能（10/10 通过）

check_page 静态体检 + 真浏览器打开：**无报错、无白屏**，可见正文 3,139 字，页面高 3,918px。

| 测试项 | 结果 |
|---|---|
| 清理面板容器 | ✅ |
| 生成计划按钮 | ✅ |
| 确认执行按钮 | ✅ |
| fetch 健康检测逻辑 | ✅ |
| fetch 计划逻辑 | ✅ |
| fetch 执行逻辑 | ✅ |
| 二次确认弹窗 | ✅ |
| 前后对比渲染 | ✅ |
| 真实数据注入 | ✅ |
| 风险标签 | ✅ |

---

## 七、测试⑤ 命令行（5/5 通过）

| 测试项 | 结果 |
|---|---|
| cli 默认排行输出 | ✅（含概览和排行） |
| cli 含风险标签 | ✅ |
| cli safe 筛选 | ✅ |
| cli 搜索 | ✅ |
| cli 权限提示 | ✅ |

---

## 八、测试中发现并排除的问题

| 现象 | 排查结论 |
|---|---|
| 测试脚本第 12 步「非法 JSON」一度显示超时 | **不是服务端问题**。单独用 curl 测，服务端 0.001 秒返回 400。是测试脚本 `q.on('error', ()=>{})` 吞掉连接错误导致假死，已改用 curl 验证 |
| 清理服务进程返回「拒绝访问」 | Windows 权限机制，非 bug。普通权限清理 Windows 服务进程会失败，需管理员权限（`启动.bat` 已自动提权） |

---

## 九、已知边界（如实说明）

1. **清理 Windows 服务进程需要管理员权限**：普通权限会如实报「拒绝访问」而非假装成功；`启动.bat` 已自动弹 UAC 提权。
2. **普通权限下约 230~240 / 400+ 个进程读不到可执行文件路径**：这是 Windows 权限机制，界面上靠进程名 + 词典兜底，提权后覆盖率显著提升。
3. **Top 30 用途覆盖率 80%**：6 个长尾应用显示「未收录」，这是如实呈现，不编造用途。
4. **释放量只在进程确实退出后才上报**：没有任何进程被关闭时释放量报 0，并注明「系统内存差值为自然波动」。

---

## 十、测试结论

**全部功能可以正确执行，47/47 项测试通过。**

- 内存采集准确（口径对齐任务管理器）
- 应用归组守恒（一个进程不丢）
- 风险分级正确（16 个保护进程零误判）
- 清理安全（防误杀实测通过、三道闸门全部拦截）
- 释放量真实（有进程退出才上报）
- 界面清晰（check_page 无报错）
- 接口健壮（404/405/400 错误处理齐全）

唯一需要在生产使用中留意的仍是**管理员权限**：清理服务进程必须以管理员身份运行（`启动.bat` 已处理）。
