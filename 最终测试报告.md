# 最终测试报告

> 测试环境：Windows 11 `10.0.26200.9457`，本机执行。
> 首次出具：2026-09-21 ｜ **最后更新：2026-09-23**
> 详细实施记录见仓库根目录 **`改动清单_实施总账.md`**。

## 结论

**评审清单 13 项改动全部完成并验证通过**，三档自动化测试全绿：单元 **122/122**、定向 HTTP **48/48**、
端到端 **152/152**（2026-09-24，D6 占用抽样探测 + D-1 闸门连续双采样修复后复跑）。
（单元测试经 D1 96→100、tokenInject +4、撤销迁移 +3、uiElevate +3、D6 +4、D-1 +4 增至 122 例，见第九节。）

未做（需人工在真机执行）：**≥24 小时循环**（事项 C，主人执行）。
已完成的原「未做」项：蓝屏压力 → V3 高 I/O 6 轮闸门实测；系统重启后链接有效 → V2.4；
界面新按钮实际点击 → V1/V2/V5 全部真机点击验证。

---

## 一、本轮实施：评审清单 13 项

按 `改动清单.md`（13 项代码改动 + 5 项测试验证类）逐项实施，每项均有对应回归断言。

| # | 优先级 | 改动 | 验证方式 | 结果 |
|---|---|---|---|---|
| 1 | P0 | 前端恒传 `force:true` 导致批量上限被绕过 | 单测 5 例 + e2e `T10 R7/R8` | 通过 |
| 2 | P0 | 磁盘禁止路径只对 C/D 盘生效 | 单测 4 例 + e2e `T5.9/T5.10` | 通过 |
| 3 | P0 | HTTP 服务无鉴权 | 定向 `G1~G13` + e2e `T11 A1~A17` | 通过 |
| 4 | P1 | 垃圾扫描盘符写死 `C:` | 单测 2 例 + e2e `T5.11/T5.12` | 通过 |
| 5 | P1 | 工作集修剪 PID 复用防护缺回退 | 单测（两处逻辑逐行比对）+ e2e `T10 R9` | 通过 |
| 6 | P1 | 磁盘应用映射表含本机硬编码 | 定向 `F3/F4`（含大小写与相似前缀实测） | 通过 |
| 7 | P1 | 释放量统计含自然波动 | 单测 6 例 + e2e `T5.13/T5.14` | 通过 |
| 8 | P2 | 垃圾词典 `note` 写死本机实测值 | 单测 2 例 + e2e `T12 P1/P2` | 通过 |
| 9 | P2 | CLI 与 Web 的 C 盘垃圾清单不一致 | 单测 1 例 + e2e `T12 P3` | 通过 |
| 10 | P2 | 磁盘应用列表硬截断无「显示全部」 | e2e `T12 P4` | 通过 |
| 11 | P2 | 服务入口注释与事实不符 | e2e `T11 A1`（源码核对） | 通过 |
| 12 | P2 | 遗留调试脚本（死代码） | 单测 1 例 + e2e `T12 P5` | 通过 |
| 13 | P2 | 清理面板注释与代码不符 | e2e `T12 P6` | 通过 |

**附带修复**：昂贵 GET 可被 `<img>` 标签反复触发（改为「默认需令牌 + 廉价接口白名单豁免」）、
`cleanup.ps1` 含非 ASCII 字节、基线清单漏 10 个源码文件、`T1` 套件清单硬编码致 5 个套件漏跑。

**对评审建议的 4 处推翻**（均基于实测，详见总账第四节）：改动 4 的「`topDirs` 改空数组」、
改动 6 的「IntelliJ 去版本号」、改动 3 的「复用 `build.js` 注入机制」、改动 13 的「本地模式隐藏面板」。

---

## 二、三档测试结果

| 档位 | 内容 | 结果 |
|---|---|---|
| 单元测试 | 14 个套件、**100 个用例** | **100 / 100 通过** |
| 定向 HTTP 验证 | 产物一致性、闸门回归、访问控制、P2 项（48 项） | **48 / 48 通过** |
| 端到端 | T0~T12，150 项 | **150 / 150 通过**（420.3s） |

### 端到端分段覆盖

| 段 | 覆盖 |
|---|---|
| T0 | 服务生命周期、源码哈希基线、基线清单完整性、生产路径未被破坏 |
| T1 | 单元测试套件自动发现（14 个）并逐个执行 |
| T2~T4 | 只读采集、内存清理闸门、沙箱真杀（含 PID 复用防护实测） |
| T5 | 磁盘只读 + 禁止路径、垃圾扫描盘符、释放量口径 |
| T6 | 沙箱真删 |
| T7~T9 | HTTP 接口、CLI、界面静态校验 |
| T10 | 历史坑回归（R1~R12） |
| T11 | 访问控制（A1~A17） |
| T12 | P2 项（P1~P6） |

### 测试资产增长

| 维度 | 实施前 | 实施后 |
|---|---|---|
| 单元测试用例 | 45 | **100**（14 套件；含缺陷 D1 新增 4 例） |
| 定向 HTTP 验证项 | 18 | **48** |
| 端到端验证项 | 108 | **150** |

> 测试脚手架（`server/services/__tests__/`）与端到端运行器均在**维护者本地**执行，不随仓库分发。
> 本地复跑（**本机需带 IO 转接层**，否则 `spawnSync powershell.exe EBUSY`；
> 2026-09-23 搬迁至 `D:\memory-cleaner` 后已补齐全部测试资产，详见
> `docs\02_测试与验收\搬迁后测试资产补齐与三档复跑报告.md`）：
> `NODE_OPTIONS="--require=<项目根>/_shim_childio.js" node --test server/services/__tests__/*.test.js`

---

## 三、任务二：内存清理安全红线（P0）

| 验收项 | 结果 | 依据 |
|---|---|---|
| 禁止 `NtSetSystemInformation` / Standby / Modified 列表清空 | 通过 | 可执行代码（`.js` / `.ps1`，去掉注释）全局扫描无命中；`safety.test.js` 锁死 |
| 禁止未公开内核 API、驱动级回收 | 通过 | 进程清理只用 `CloseMainWindow` / `taskkill`；修剪只用 `psapi.EmptyWorkingSet` 与 `kernel32.SetProcessWorkingSetSize` |
| 禁止磁盘大量读写期间自动触发清理 | 通过（手动路径） | 无常驻自动强清；真执行前 `assertDiskIdle`（≥20MB/s 或队列≥3 返回 HTTP 409 `DISK_BUSY`） |
| 仅公开 API 工作集修剪 | 通过 | `POST /api/cleanup/trim`，界面「修剪工作集（不关程序）」 |
| 手动触发 + 风险提示 | 通过 | 确认框写明「不会调用未公开内核 API / 不会清空 Standby」；默认 dry-run、需 `confirmed=true` |
| 代码审查单测 | 通过 | `safety.test.js` + `workingSet.test.js` + `diskIoGuard.test.js` |
| 大文件下载期间反复清理、无蓝屏 | **通过（2026-09-24 V3 真机）** | 高 I/O 下 6 轮采样 **5 次正确拒绝 `DISK_BUSY`**、空闲 2/2 放行、系统稳定无蓝屏；观察项 O1 已修（闸门改连续双采样，任一轮忙即拒绝） |
| ≥24 小时循环稳定 | **未测** | 事项 C，待主人挂机执行 |

---

## 四、任务一：缓存目录迁移

| 验收项 | 结果 | 依据 |
|---|---|---|
| 预检（空间 / 目标不在源内 / 关键目录） | 通过 | `precheck`；空间不足 `DISK_FULL`，源内目标 `DEST_INSIDE_SOURCE` |
| 复制后校验数量/大小 | 通过 | 不一致抛 `COPY_MISMATCH` 并回滚 |
| 原目录改名备份，不直接删除 | 通过 | `源.__ccbak_<时间戳>__`，默认保留 7 天 |
| 默认 junction（`mklink /J`），可选 symlink `/D` | 通过 | `linkType` 默认 `junction` |
| 探针：经原路径写入，确认落在目标 | 通过 | `probeLink`；沙箱真迁用例写入 `via-link.txt` 在目标盘读到 |
| 失败自动回滚，原数据不丢 | 通过 | `rollback`：拆链接、还原备份、删不完整副本 |
| 占用中途失败明确报错 | 通过 | `SOURCE_IN_USE`；界面提示先关占用程序 |
| 目标重名拒绝 | 通过 | `DEST_EXISTS` |
| 检查器区分普通目录 / junction / symlink / 断链 | 通过（沙箱） | `inspect`；沙箱验证 junction；symlink/断链字段具备，真实断链需手工造链再看 |
| 拒绝 Windows / System32 / Program Files / 用户配置根 | 通过 | `isCriticalPath` 单测 |
| 迁移后程序照常写原路径、C 盘释放 | 通过（沙箱 `%TEMP%`） | 未在本机真实浏览器缓存上搬家（避免动生产数据） |
| 重启后链接仍有效 | **未测** | junction 是 NTFS 重解析点，重启应仍在；需迁一条真实缓存后重启确认 |

---

## 五、单元测试明细

| 范围 | 结果 |
|---|---|
| 全量套件（14 个） | **96/96 通过** |
| └ 归组守恒、风险分级、内存、磁盘、提权、扫尾时序等原有套件 | 通过 |
| └ 禁用内核 API 扫描、磁盘忙碌闸门、工作集修剪闸门 | 通过 |
| └ 缓存搬走沙箱真迁（`mklink /J` + 探针 + 回滚拒绝项） | 9/9 通过 |
| └ 内存条 0 / 1 / N 条 | 通过 |
| └ 批量上限独立闸门、磁盘禁止路径全盘符、垃圾扫描盘符动态化、PID 复用回退、释放量口径 | 12/12 通过 |
| └ 词典不写死本机值、CLI 与服务端垃圾清单同源、死代码清除 | 4/4 通过 |

---

## 六、恒真判断与物理内存条

`cacheMigrateService.js` 里原来的

```js
if (linkType === 'junction' && !sameVolume(source, destination) === false)
```

**已删除**。预检只保留「junction 时必须识别出盘符」。无调用的 `sameVolume()` 也已删。

物理内存条不是写死本机：`Win32_PhysicalMemory` 现场读；PowerShell 5.1 单条会收成对象，
采集脚本 `@($modules)` + Node `asArray()` 已收口。0 / 1 / N 条单测通过。

---

## 七、已知限制

1. ~~真实浏览器/游戏缓存搬家未在本机生产路径执行~~ → **已解除（2026-09-24 V2）**：Edge 缓存真机迁移成功
   （1099 文件/379MB，junction，重启后 `broken=false`），并提供「撤销迁移」正规回滚通道（`rolledBack` 标记）。
2. **≥24 小时循环未做**（事项 C）。蓝屏压力 → 已由 V3 高 I/O 闸门实测覆盖；
   系统重启后链接有效 → V2.4 已验证。迁移预检占用抽样（D6）为**抽样结论**，
   标注「基于当前时刻」——占用状态可能随后续程序运行变化。
3. 符号链接 `/D` 在未开开发者模式、非管理员时会 `LINK_FAILED` 并回滚。
4. 工作集修剪不结束进程，任务管理器「已用」不一定等量下降。
5. 磁盘忙碌阈值是经验值（20MB/s / 队列 3），极端低速拷贝可能漏过闸门。
6. **访问控制是「抬高门槛」不是绝对隔离**：令牌靠首页下发，外部网页受同源策略限制读不到它；
   但本机程序仍可读取本地文件或首页拿到令牌。要做到真正的强隔离，需改用命名管道等进程间通道。
7. 释放量按「各成功进程的工作集」统计，会低估「进程退出后系统连带回收」的那部分内存——
   这是刻意选择的保守方向。

---

## 八、建议下一步

1. `启动.bat` 打开界面，磁盘页试「缓存搬走」预检（先预检，不要对正在用的缓存点确认）。
2. 空闲时对 `%TEMP%` 或一条可重建的缓存做一次真实搬走，再重启看链接。
3. 若要验证「下载时不蓝屏」：开一个大文件下载，点结束进程或修剪，应看到「磁盘正忙」而不是清理成功；不应出现蓝屏。
4. 手动点一遍本轮受影响的界面：磁盘应用的「显示全部」、清理面板在本地文件模式下的提示。

---

## 九、后续修复：缺陷 D1（2026-09-23 晚）

做真机验收前的**零风险预检**时发现并修复的一条真实缺陷。

### 9.1 缺陷

`server/services/cacheMigrateService.js` 的 `precheck()` 对关键路径**只记录 issue、不短路返回**，
随后仍无条件执行 `walkStats(source)` 做同步递归遍历。把 `C:\Windows` 填进源目录时，
服务会同步遍历数十万文件 —— **事件循环被完全阻塞**，期间连 `GET /api/health` 都不响应，
连接堆积为 `CLOSE_WAIT`，遍历完才自行恢复（约 1~3 分钟）。

| 维度 | 说明 |
|---|---|
| 严重度 | **P2**（功能可用，但缺乏输入防护） |
| 数据风险 | **零** —— 只遍历统计，不写不删 |
| e2e 未覆盖原因 | 迁移用例全部使用 `%TEMP%` 沙箱**小目录**，从未使用「关键路径 + 大目录」组合 |

### 9.2 修复

| 改动 | 内容 |
|---|---|
| `walkStats` | 新增「耗时 + 文件数」双上限（`WALK_MAX_MS=3000` / `WALK_MAX_FILES=200000`），超限停止遍历并返回 `truncated` |
| `precheck` | 改为两阶段：阶段 1 只做轻量检查（路径 / 元数据）；**阶段 2 仅在无阻断问题时**才做重量级统计 |
| 新增错误码 | `SOURCE_TOO_LARGE` —— 目录规模超限时提示改选具体的缓存子目录 |

**兼容性**：`execute()` 在 `check.ok === false` 时直接抛错、**不消费 `stats`**，
仅在 `ok === true` 时使用，故两阶段改造对执行路径完全兼容。

### 9.3 验证

| 证据 | 结果 |
|---|---|
| 新增回归测试 | 4 例（D1-a ~ D1-d），`cacheMigrate` 套件 9 → 13 例 |
| **负向验证** | 临时还原修复前版本重跑 → D1-d **确实变红**（证明断言不是「永远通过」） |
| 真机实测 | `C:\Windows` 由「数十秒~数分钟无响应」→ **0.00s 返回 400 + `CRITICAL_PATH`**；连续三次调用后 `GET /api/health` 仍正常响应 |
| 非关键路径对照 | `C:\Windows\Temp` 走完整遍历，1.31s 返回 `ok=true` |
| 基线重建 | `regenerate-baseline.js` 重建 48 条目 |
| 三档复跑 | 单元 **100/100**、定向 HTTP **48/48**、端到端 **150/150**（447.2s） |

完整过程记录见 `docs\02_测试与验收\零风险预检报告.md`。
